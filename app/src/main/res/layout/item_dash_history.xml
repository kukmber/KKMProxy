<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:background="@drawable/bg_card"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="14dp"
    android:paddingTop="12dp"
    android:paddingEnd="6dp"
    android:paddingBottom="12dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/historyPin"
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:layout_marginEnd="6dp"
                android:importantForAccessibility="no"
                android:src="@drawable/ic_k_pin"
                app:tint="@color/accent2" />

            <TextView
                android:id="@+id/historyName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/text"
                android:textSize="13.5sp"
                android:textStyle="bold"
                tools:text="YouTube" />

            <TextView
                android:id="@+id/historyActive"
                style="@style/Dash.StatusTag"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_status_tag_on"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:text="@string/editor_active"
                android:textColor="@color/accent"
                android:textSize="10sp" />
        </LinearLayout>

        <TextView
            android:id="@+id/historyCommand"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:ellipsize="end"
            android:fontFamily="monospace"
            android:maxLines="3"
            android:textColor="@color/muted"
            android:textSize="11.5sp"
            tools:text="-o1 -Ar -At -f-1 -r1+s -As" />
    </LinearLayout>

    <ImageView
        android:id="@+id/historyMore"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginStart="4dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/cmd_history_menu"
        android:padding="8dp"
        android:src="@drawable/ic_k_more"
        app:tint="@color/muted" />
</LinearLayout>
